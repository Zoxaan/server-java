
package ru.biponline.demo.utils;

import ru.biponline.demo.entity.ClientsEntity;
import ru.biponline.demo.entity.OrdersEntity;
import ru.biponline.demo.entity.ProductsEntity;
import ru.biponline.demo.exception.ValidationExceptionOrders;
public class ValidationUtils {
    public static void ValidationClients(OrdersEntity orders) throws ValidationExceptionOrders {
//        String ordersTitle = orders.getOrders();
//        if(ordersTitle == null || ordersTitle.isBlank() || ordersTitle.length() < 3 || ordersTitle.length() > 35)
//            throw new ValidationExceptionOrders("Название книги должно быть в приделах от 3 до 35 символов");
//        ClientsEntity clients = orders.getClients();
//        if(clients == null) //|| author.getName().isBlank() || author.getName().length() < 3 || author.getName().length() > 15
//            throw new ValidationExceptionOrders("Имя автора должно быть в приделах от 4 до 15");
//        ProductsEntity products = orders.getProducts();
//        if (products == null) //|| publishing.getPublishing().isBlank() || publishing.getPublishing().length() < 3 || publishing.getPublishing().length() > 30
//            throw new ValidationExceptionOrders("Название издательства должна быть от 3 до 30 символов");
    }
}

